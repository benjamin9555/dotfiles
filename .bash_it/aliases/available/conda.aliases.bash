cite 'about-alias'
about-alias 'personal conda abbreviations'

# Aliases
alias coa='conda activate'
alias cod='conda deactivate'
alias coel='conda env list'
alias cocn='conda create -n'
alias coerm='conda env remove -n'
alias coexp='conda env export --no-builds >'
